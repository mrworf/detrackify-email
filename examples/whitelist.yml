# Whitelist configuration for Detrackify
# This file contains URLs that are always allowed

# Whitelist entries - URLs that are always allowed
whitelist:
  - url: 'https://trusted.example.com/logo.png'
  - url: 'https://cdn.example.org/.*' 